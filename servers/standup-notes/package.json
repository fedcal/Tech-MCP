{
  "name": "@mcp-suite/server-standup-notes",
  "version": "0.1.0",
  "description": "MCP server: standup-notes",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "mcp-standup-notes": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "test": "vitest run",
    "test:unit": "vitest run tests/",
    "lint": "eslint src/",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@mcp-suite/client-manager": "workspace:*",
    "@mcp-suite/core": "workspace:*",
    "@mcp-suite/database": "workspace:*",
    "@mcp-suite/event-bus": "workspace:*",
    "better-sqlite3": "^11.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@mcp-suite/testing": "workspace:*",
    "@types/better-sqlite3": "^7.6.12",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
